pipeline {
    agent any

/*    stages {
        stage('Clone Repositorio') {
            steps {
                git branch: 'main', url: 'https://github.com/laurabecerra1502/devops.git'
            }
       }

        stage('Load Jenkinsfile') {
            steps {
                script {
                    load 'jenkinsfile'
                }
            }
        }
    }
*/
        stages {
            stage('Create Job using Job DSL') {
                steps {
                    jobDsl targets: ['jobdsl.groovy']
            }
        }
    }
}
